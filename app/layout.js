import './globals.css';

export const metadata = {
  title: 'UAE Job Intelligence',
  description: 'Live UAE job discovery with candidate-fit scoring and application intelligence.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
